---
id: "06-04"
title: "Tests — tests/test_mapping.py and tests/test_study_ui.py"
wave: 3
depends_on: ["06-01", "06-02", "06-03"]
files_modified:
  - tests/test_mapping.py
  - tests/test_study_ui.py
autonomous: true
requirements_addressed: ["GEN-01", "GEN-02", "EXP-V2-01"]
---

<objective>
Write tests for the Phase 6 deliverables: tests/test_mapping.py covers
OracleRuleSet, extract_oracle_rules (with stub JSONL), MappingProtocol
isinstance checks, LLMMappingStrategy (mocked LLM), and CentroidMappingStrategy
(synthetic embeddings). tests/test_study_ui.py covers the POST /study/sessions
endpoint, GET /study/{session_id}, satisfaction detection logic, and STUDY_MAX_TURNS cap.

Purpose: Verify core logic without live LLM calls and without network round-trips.

Output: tests/test_mapping.py and tests/test_study_ui.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-generalization-and-human-validation/06-CONTEXT.md
@.planning/phases/06-generalization-and-human-validation/06-01-SUMMARY.md
@.planning/phases/06-generalization-and-human-validation/06-03-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From tests/test_db.py (existing test pattern to follow):
- pytest fixtures with conftest.py or inline fixtures
- sqlite3.connect(":memory:", check_same_thread=False) for DB tests
- No live LLM calls in tests — mock anthropic.Anthropic() client

From src/mapping.py (06-01 output):
- OracleRuleSet, MappingProtocol, LLMMappingStrategy, CentroidMappingStrategy
- MAPPING_REGISTRY, extract_oracle_rules

From src/state.py:
- ClusteringState, Cluster
- Construct minimal test state:
    clusters = [Cluster(id=0, name="A", description="desc A", item_ids=[0, 1]),
                Cluster(id=1, name="B", description="desc B", item_ids=[2, 3])]
    assignments = {0: 0, 1: 0, 2: 1, 3: 1}
    soft_probs = {0: [0.9, 0.1], 1: [0.8, 0.2], 2: [0.1, 0.9], 3: [0.2, 0.8]}
    state = ClusteringState(turn_index=1, timestamp="2026-01-01T00:00:00+00:00",
                            clusters=clusters, assignments=assignments, soft_probs=soft_probs)

From src/embedding_store.py:
- EmbeddingStore(np.ndarray) — construct with synthetic 4×384 float32 array for tests
  embeddings = np.random.default_rng(42).random((4, 384)).astype(np.float32)
  store = EmbeddingStore(embeddings)

FastAPI test client:
- from fastapi.testclient import TestClient
- client = TestClient(web.app.app)  (the FastAPI app object, not asgi_app)
- client.post("/study/sessions", json={"dataset_path": path, "backend": "hdbscan"})
- client.get("/study/test-session-id")
</interfaces>
</context>

<tasks>

<task id="T-06-04-01" title="tests/test_mapping.py — unit tests for src/mapping.py">
  <read_first>
    - src/mapping.py                     (all exports to test)
    - src/state.py                       (ClusteringState, Cluster construction)
    - src/embedding_store.py             (EmbeddingStore constructor)
    - tests/test_db.py                   (pytest patterns used in this project)
    - .planning/phases/06-generalization-and-human-validation/06-CONTEXT.md  (D-01 through D-05)
  </read_first>
  <action>
    Create tests/test_mapping.py with these test functions:

    Helper: _make_test_state() -> ClusteringState
      Builds a minimal 4-item, 2-cluster state as shown in the interfaces block above.

    Helper: _make_test_store() -> EmbeddingStore
      Creates a 4×384 float32 EmbeddingStore with rng(42) random data.
      Cluster 0 items (0, 1) have embeddings with high values on dim 0;
      Cluster 1 items (2, 3) have embeddings with high values on dim 1.
      Use: emb = np.zeros((4, 384), dtype=np.float32)
            emb[0, 0] = 1.0; emb[1, 0] = 0.9  # cluster 0
            emb[2, 1] = 1.0; emb[3, 1] = 0.9  # cluster 1

    Helper: _make_rule_set() -> OracleRuleSet
      OracleRuleSet(synonyms=[["error", "fail"]], focus_areas=["billing"],
                    exclusions=[], cluster_rules=["technical issues go in A"])

    Test functions:

    def test_oracle_rule_set_fields():
      r = OracleRuleSet(synonyms=[["a","b"]], focus_areas=["x"], exclusions=[], cluster_rules=[])
      assert r.synonyms == [["a", "b"]]
      assert r.focus_areas == ["x"]
      assert r.exclusions == []
      assert r.cluster_rules == []

    def test_mapping_registry_keys():
      assert set(MAPPING_REGISTRY) == {"llm", "centroid"}

    def test_mapping_protocol_isinstance():
      rule_set = _make_rule_set()
      assert isinstance(LLMMappingStrategy(), MappingProtocol)
      assert isinstance(CentroidMappingStrategy(), MappingProtocol)

    def test_extract_oracle_rules_empty_audit_log(tmp_path, monkeypatch):
      # Write an audit_log.jsonl with NO instructional deltas
      import json
      audit_path = str(tmp_path / "audit_log.jsonl")
      # Write one turn with only global delta
      line = json.dumps({"turn_index": 0, "deltas": [{"type": "global", "instruction_text": "too many"}]})
      (tmp_path / "audit_log.jsonl").write_text(line + "\n")
      # Should call deviation() and return empty OracleRuleSet
      deviations = []
      monkeypatch.setattr("src.mapping.deviation", lambda msg, **kw: deviations.append(msg))
      result = extract_oracle_rules(audit_path)
      assert result.synonyms == []
      assert result.focus_areas == []
      assert len(deviations) == 1
      assert "no_fb04_entries" in deviations[0]

    def test_centroid_strategy_assigns_correctly(monkeypatch):
      # Item with embedding close to cluster 0 centroid should be assigned to cluster 0
      state = _make_test_state()
      store = _make_test_store()
      rule_set = _make_rule_set()
      strategy = CentroidMappingStrategy()
      # Mock SentenceTransformer.encode to return a vector close to cluster 0 centroid
      import numpy as np
      test_vec = np.array([[1.0] + [0.0] * 383], dtype=np.float32)  # close to cluster 0
      monkeypatch.setattr(strategy._model, "encode",
                          lambda texts, **kw: test_vec)
      result = strategy.assign("some item text", state, rule_set, store)
      assert result == "0"  # cluster 0 has centroid with high dim-0 value

    def test_centroid_strategy_empty_cluster_raises():
      import pytest
      import numpy as np
      from src.state import ClusteringState, Cluster
      # Build state where cluster 1 has no items
      clusters = [Cluster(id=0, name="A", description="d", item_ids=[0]),
                  Cluster(id=1, name="B", description="d", item_ids=[])]  # EMPTY
      assignments = {0: 0}
      soft_probs = {0: [1.0, 0.0]}
      state = ClusteringState(turn_index=0, timestamp="2026-01-01T00:00:00+00:00",
                              clusters=clusters, assignments=assignments, soft_probs=soft_probs)
      emb = np.ones((1, 384), dtype=np.float32)
      store = EmbeddingStore(emb)
      strategy = CentroidMappingStrategy()
      rule_set = _make_rule_set()
      # monkeypatch not needed — ValueError raised before strategy reads embeddings
      with pytest.raises(ValueError, match="has no items"):
          strategy.assign("text", state, rule_set, store)

    def test_llm_strategy_validates_cluster_id(monkeypatch):
      import pytest
      state = _make_test_state()
      store = _make_test_store()
      rule_set = _make_rule_set()
      strategy = LLMMappingStrategy()
      # Mock LLM to return a cluster ID not in state.clusters
      class FakeMessage:
          text = "99"  # not a valid cluster ID
      class FakeContent:
          content = [FakeMessage()]
      class FakeClient:
          class messages:
              @staticmethod
              def create(**kw):
                  return FakeContent()
      monkeypatch.setattr("src.mapping.anthropic.Anthropic", lambda: FakeClient())
      with pytest.raises(ValueError, match="unknown cluster id"):
          strategy.assign("item text", state, rule_set, store)

    Run command: python -m pytest tests/test_mapping.py -v
    All tests must pass without live LLM calls (all LLM interactions are monkeypatched).
  </action>
  <acceptance_criteria>
    - tests/test_mapping.py exists
    - python -m pytest tests/test_mapping.py -v exits 0 (all tests pass)
    - test_oracle_rule_set_fields, test_mapping_registry_keys, test_mapping_protocol_isinstance present
    - test_extract_oracle_rules_empty_audit_log present and verifies deviation() called with "no_fb04_entries"
    - test_centroid_strategy_empty_cluster_raises present and verifies ValueError with "has no items"
    - test_llm_strategy_validates_cluster_id present and verifies ValueError on unknown cluster ID
    - No test makes a live anthropic API call (all LLM surfaces are monkeypatched)
  </acceptance_criteria>
</task>

<task id="T-06-04-02" title="tests/test_study_ui.py — FastAPI endpoint tests for study routes">
  <read_first>
    - web/app.py                         (study routes, _study_sessions structure)
    - tests/test_db.py                   (test patterns, fixtures)
    - .planning/phases/06-generalization-and-human-validation/06-CONTEXT.md  (D-12, D-13, D-14)
    - .planning/phases/04-judge-agent/04-CONTEXT.md  (D-01: in-memory DB for tests)
  </read_first>
  <action>
    Create tests/test_study_ui.py with these tests:

    Fixture: test_dataset(tmp_path) -> str
      Create a minimal JSONL dataset file with 10 records in tmp_path/dataset.jsonl
      Each record: {"text": f"item text number {i}"} for i in range(10)
      Return str(tmp_path / "dataset.jsonl")

    Fixture: study_client()
      from fastapi.testclient import TestClient
      from web.app import app
      Use TestClient(app) — NOT asgi_app (TestClient works with the FastAPI app directly)

    Test functions:

    def test_post_study_sessions_valid(test_dataset, study_client, monkeypatch):
      # Monkeypatch threading.Thread.start to prevent background thread from actually running
      monkeypatch.setattr("threading.Thread.start", lambda self: None)
      # Monkeypatch DB operations to use in-memory DB
      import sqlite3
      mem_db = sqlite3.connect(":memory:", check_same_thread=False)
      monkeypatch.setattr("web.app.connect", lambda: mem_db)
      monkeypatch.setattr("web.app.init_schema", lambda db: None)
      # Monkeypatch experiments.create to return a fake ExperimentRead
      from unittest.mock import MagicMock
      fake_exp = MagicMock(); fake_exp.id = 1
      monkeypatch.setattr("web.app.experiments.create", lambda db, ec: fake_exp)
      resp = study_client.post("/study/sessions",
                               json={"dataset_path": test_dataset, "backend": "hdbscan"})
      assert resp.status_code == 200
      data = resp.json()
      assert "session_id" in data
      assert "study_url" in data
      assert data["study_url"].startswith("/study/")

    def test_post_study_sessions_nonexistent_dataset(study_client):
      resp = study_client.post("/study/sessions",
                               json={"dataset_path": "/nonexistent/path.jsonl", "backend": "hdbscan"})
      assert resp.status_code == 400

    def test_post_study_sessions_invalid_backend(test_dataset, study_client):
      resp = study_client.post("/study/sessions",
                               json={"dataset_path": test_dataset, "backend": "invalid"})
      assert resp.status_code == 400

    def test_get_study_page_known_session(study_client, monkeypatch):
      # Pre-populate _study_sessions with a fake session ID
      import web.app as app_module
      app_module._study_sessions["test-session-99"] = {"state": None, "ended": False}
      resp = study_client.get("/study/test-session-99")
      assert resp.status_code == 200
      assert b"study" in resp.content.lower()  # page content mentions "study"

    def test_get_study_page_unknown_session(study_client):
      resp = study_client.get("/study/nonexistent-session-xyz")
      assert resp.status_code == 404

    def test_study_max_turns_env_default(monkeypatch):
      monkeypatch.delenv("STUDY_MAX_TURNS", raising=False)
      import importlib, web.app as app_module
      # Re-read the constant (module already loaded; just verify the value)
      # STUDY_MAX_TURNS should be 30 when env var not set
      assert app_module.STUDY_MAX_TURNS == 30

    def test_study_max_turns_env_override(monkeypatch):
      monkeypatch.setenv("STUDY_MAX_TURNS", "5")
      # Reimporting just verifies the reading logic; but constant is set at module load.
      # Test the int() parsing directly from the source expression:
      import os
      result = int(os.environ.get("STUDY_MAX_TURNS", "30"))
      assert result == 5

    Run command: python -m pytest tests/test_study_ui.py -v
    All tests must pass. No live DB writes. No live LLM calls.
  </action>
  <acceptance_criteria>
    - tests/test_study_ui.py exists
    - python -m pytest tests/test_study_ui.py -v exits 0 (all tests pass)
    - test_post_study_sessions_valid asserts status 200 and response has session_id + study_url
    - test_post_study_sessions_nonexistent_dataset asserts status 400
    - test_post_study_sessions_invalid_backend asserts status 400
    - test_get_study_page_known_session asserts status 200
    - test_get_study_page_unknown_session asserts status 404
    - No test makes a live anthropic API call or live DB connection
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → monkeypatched functions | All external IO is mocked; tests never hit live LLM or DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | Tampering | Live LLM call in tests | mitigate | all anthropic.Anthropic() calls monkeypatched in test functions |
| T-06-04-02 | Tampering | Live DB writes in UI tests | mitigate | connect() monkeypatched to in-memory sqlite3; threading.Thread.start patched to no-op |
| T-06-04-SC | Tampering | pip installs for test deps | mitigate | pytest already installed; httpx (FastAPI TestClient dep) already in dev deps |
</threat_model>

## Verification

**Must-haves for test coverage:**

1. `python -m pytest tests/test_mapping.py -v` exits 0 — all mapping tests pass
2. `python -m pytest tests/test_study_ui.py -v` exits 0 — all study UI tests pass
3. `python -m pytest tests/test_mapping.py tests/test_study_ui.py -v` exits 0 — full combined run
4. No test makes a live API call (grep: "monkeypatch" or "MagicMock" present in every test that touches LLM)
5. test_centroid_strategy_empty_cluster_raises verifies the ValueError behavior from T-06-01-02

<success_criteria>
- tests/test_mapping.py: 6+ test functions, all pass
- tests/test_study_ui.py: 6+ test functions, all pass
- Full suite runs in under 30 seconds (no live LLM or network calls)
- Coverage includes: OracleRuleSet validation, extract_oracle_rules empty case, MAPPING_REGISTRY keys,
  MappingProtocol isinstance, CentroidMappingStrategy assignment logic, LLMMappingStrategy invalid ID,
  POST /study/sessions valid/invalid cases, GET /study/{id} known/unknown
</success_criteria>

<output>
Create .planning/phases/06-generalization-and-human-validation/06-04-SUMMARY.md when done.
</output>
