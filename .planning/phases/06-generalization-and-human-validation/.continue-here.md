---
context: phase
phase: 06-generalization-and-human-validation
task: 5
total_tasks: 5
status: code_complete_research_pending
last_updated: 2026-05-17T09:45:00Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

_No blocking constraints discovered through failure this session. See Advisory section below._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Stale experiments.db | Pre-Phase-5 DB lacks oracle_type column; CREATE TABLE IF NOT EXISTS silently skips new DDL | advisory | Delete experiments.db at repo root before first harness run |
| session_id collision | _make_session_timestamp() used naive datetime.now() — fixed in CR-04; sessions created within same second overwrote each other | advisory | Fixed: now uses UTC + secrets.token_hex(4) suffix |

<current_state>
Phase 6 — Generalization and Human Validation

All 5 plans executed and all 12 code review findings fixed. Verification ran and returned
gaps_found (6/8 must-haves). The 2 gaps are RESEARCH EXECUTION GAPS, not code gaps:
- Gap 1: Human study N>=10 not completed (study UI is built; no participants have run yet)
- Gap 2: LLM-vs-human gap not quantified (compare_oracle_types.py works; DB has no human rows)

The code is correct and ready. The science needs to be conducted.
</current_state>

<completed_work>
All Phase 6 code plans:
- 06-01: src/mapping.py — OracleRuleSet, extract_oracle_rules, MappingProtocol, LLMMappingStrategy, CentroidMappingStrategy, MAPPING_REGISTRY (GEN-01) ✓
- 06-02: examples/evaluate_mapping.py — 25-item held-out evaluation, both strategies, bootstrap CI (GEN-02) ✓
- 06-03: web/app.py + web/templates/study.html + web/static/study.js — human study UI with /study routes, satisfaction detection, 30-turn cap, oracle_type='human' DB writes (EXP-V2-01) ✓
- 06-04: tests/test_mapping.py + tests/test_study_ui.py — 14 tests green ✓
- 06-05: examples/compare_oracle_types.py + notebooks/llm_vs_human.ipynb (EXP-V2-01) ✓

Code review (standard depth, 8 files):
- 4 Critical + 5 Warning + 3 Info found
- All 12 fixed and committed (commits fecf034..7b153a2)

Key fixes applied:
- CR-01: feedback_queue.pop(0) now guarded — prevents IndexError killing study daemon thread
- CR-02: _detect_satisfaction re-raises APIError (fail loudly)
- CR-03: int(prediction) wrapped in try/except ValueError in evaluate_mapping.py
- CR-04: _make_session_timestamp() uses datetime.now(timezone.utc) + token_hex(4)
- WR-04: turn_index incremented in satisfaction-rejected branch
- WR-05: init_schema removed from read-only compare_oracle_types CLI
- IN-01: print() → log.debug/log.info in web/app.py
</completed_work>

<remaining_work>
RESEARCH TASKS (not code — need human action):

1. Run LLM harness to populate DB:
   python -m examples.run_harness --config experiments/configs/harness.yaml

2. Conduct human study (N>=10 participants):
   uvicorn web.app:asgi_app --host 0.0.0.0 --port 5000
   → direct participants to http://localhost:5000/study

3. Run mapping evaluation against real session:
   python -m examples.evaluate_mapping --session-dir sessions/<ts>/

4. Quantify LLM-vs-human gap:
   python -m examples.compare_oracle_types

5. Re-verify Phase 6:
   /gsd-verify-work 6
</remaining_work>

<decisions_made>
- Ground-truth labeling in evaluate_mapping.py uses direct Anthropic client call, not OracleAgent — avoids noise params contaminating labeling accuracy (D-06)
- _make_session_timestamp() now UTC + 8-char hex suffix — solves same-second collision and timezone skew
- _detect_satisfaction re-raises APIError — fail loudly; caller handles it
- Verification status is gaps_found, not failed — all code checks pass; gaps are research execution only
- No new packages added in Phase 6 (pydantic, anthropic, sentence_transformers already in requirements.txt)
</decisions_made>

<blockers>
- Human study N>=10 NOT YET RUN — experiments.db has no oracle_type='human' rows
  → Blocks Phase 6 completion; study UI is ready at /study
- LLM-vs-human gap not quantified — needs both LLM + human rows in DB
  → Run harness first, then human study, then compare_oracle_types.py
</blockers>

## Required Reading (in order)
1. `.planning/phases/06-generalization-and-human-validation/06-VERIFICATION.md` — full list of 6/8 verified must-haves and the 2 gap descriptions
2. `.planning/phases/06-generalization-and-human-validation/06-REVIEW.md` — original findings (all fixed; read for context on decisions made)
3. `./CLAUDE.md` — project coding philosophy (fail loudly, FastAPI stack, DB constraints, deviation() usage)

## Infrastructure State
- Web server: NOT running (must be started manually for human study)
  `uvicorn web.app:asgi_app --host 0.0.0.0 --port 5000 --reload`
- experiments.db: exists at repo root; contains LLM oracle rows from Phase 5 runs; NO human rows yet
- ANTHROPIC_API_KEY: must be set in env before running harness or study (oracle LLM calls)
- All 14 Phase 6 tests pass: `python -m pytest tests/test_mapping.py tests/test_study_ui.py`

<context>
This session executed all 5 Phase 6 plans (code infrastructure complete), ran a standard
code review (12 findings), fixed all findings in 12 atomic commits, and ran verification
which returned gaps_found. The 2 gaps are not code bugs — they're the actual research that
hasn't been conducted yet: the human study needs real participants, and the comparison analysis
needs real data. Once the human study is run, /gsd-verify-work 6 should pass.

The notebook (notebooks/llm_vs_human.ipynb) has placeholder cells (Key Finding, per-strategy
breakdown) that will be filled in after the study data is collected.
</context>

<next_action>
Start with: Run `python -m examples.run_harness --config experiments/configs/harness.yaml`
to generate LLM baseline data, then conduct the human study via the web UI (N>=10),
then run /gsd-verify-work 6.
</next_action>
