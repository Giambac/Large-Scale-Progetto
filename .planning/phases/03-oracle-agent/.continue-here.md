---
context: phase
phase: 03-oracle-agent
task: 0
total_tasks: 0
status: ready_to_execute
last_updated: 2026-05-11T20:30:00Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints. Planning completed cleanly with no failures encountered._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| oracle_init/drift_event in audit_log.jsonl | load_audit_log() calls deserialize_state() on every line and asserts turn_index — non-state records cause AssertionError crash | advisory | Write oracle_init and drift_event to sessions/<timestamp>/events.jsonl sidecar ONLY — never to audit_log.jsonl. Plans 01 and 04 enforce this. |
| system= kwarg on OpenAI/Google adapters | _OpenAIAdapter and _GoogleAdapter do not accept system= kwarg — passing it causes TypeError | advisory | OracleAgent._build_system_prompt() uses isinstance branch: Anthropic gets system= kwarg; others get system prompt prepended to first user message (Plan 01 Task 1). |
| update_delta_window before f_next_state | Calling with state.turn_index instead of new_state.turn_index corrupts contradicted_turn references in events.jsonl (off-by-one) | advisory | Plan 04 Task 2 Modification B calls update_delta_window AFTER f_next_state, using new_state.turn_index. |

<current_state>
Phase 3 planning is COMPLETE. All 5 plans created, verified by plan-checker, and committed (94a1e7e).

No execution has started yet — Wave 0 test scaffold has not been run.

Plans:
- 03-00 (Wave 0): Test scaffold — 4 test files + oracle_agent_factory fixture in conftest.py
- 03-01 (Wave 1): src/oracle_agent.py — OracleSpec, NoiseParams, OracleAgent, _build_system_prompt, oracle_init logging
- 03-02 (Wave 1): src/cognitive_load.py — f_cognitive_load pure function, named constants (parallel with 03-01)
- 03-03 (Wave 2): drift detection — _contradicts(), update_delta_window(), deque(maxlen=10)
- 03-04 (Wave 3): loop integration — f_cognitive_load wired in, oracle_init/drift_event to events.jsonl, FB-04 global_instructions

Ready to execute: /gsd-execute-phase 3
</current_state>

<completed_work>

**This session:**
- Ran /gsd-resume-work — detected Phase 3 CONTEXT.md ready (prior session work)
- Ran /gsd-plan-phase 3:
  - Research: 03-RESEARCH.md written (Pitfall 5 events.jsonl sidecar, system= kwarg compat, merged cluster ID convention, validation architecture)
  - Pattern mapping: 03-PATTERNS.md written (9 file analogs found)
  - Planning: 5 plans created (03-00 through 03-04)
  - Verification: 4 blockers found → 1 revision round → all resolved → VERIFICATION PASSED
  - 03-VALIDATION.md created (Nyquist test map, 17 test entries, Wave 0 gaps)
- Committed: docs(03): create phase plan (94a1e7e)

**Prior session (2026-05-11):**
- Ran /gsd-discuss-phase 3 — 12 decisions locked in 03-CONTEXT.md (D-01..D-12)
- Committed: docs(03): capture phase context (74ee4fc)
</completed_work>

<remaining_work>

**All of Phase 3 execution:**
- Wave 0: 03-00 — test scaffold (tests/phase3/ directory, 4 test files, conftest.py update)
- Wave 1: 03-01 + 03-02 in parallel — OracleAgent + f_cognitive_load
- Wave 2: 03-03 — drift detection
- Wave 3: 03-04 — loop integration

Requirements to satisfy: ORC-01, ORC-02, ORC-03, ORC-04, FB-04
</remaining_work>

<decisions_made>

**Planning decisions resolved this session (not in CONTEXT.md — discovered during research):**
- oracle_init + drift_event → events.jsonl sidecar (load_audit_log() crash avoidance — Pitfall 5)
- Anthropic system= vs OpenAI/Google prepend — isinstance branch in _build_system_prompt()
- Merged cluster ID: SplitFeedback(X) contradicts MergeFeedback(A,B) when X==A or X==B (both retired input IDs, not the new merged ID)
- OracleAgent.__init__ writes oracle_init directly when events_path provided (enables unit testing without the loop)
- update_delta_window called after f_next_state, uses new_state.turn_index

**CONTEXT.md decisions (D-01..D-12) — all remain as specified:**
- See .planning/phases/03-oracle-agent/03-CONTEXT.md for full decision log
</decisions_made>

<blockers>
None. One non-blocking advisory:
- hdbscan not installable on Python 3.14/Windows — 1 Phase 2 test skips via importorskip guard. Does not affect Phase 3 (no hdbscan in oracle agent code).
</blockers>

## Required Reading (in order)
1. `.planning/phases/03-oracle-agent/03-CONTEXT.md` — ALL decisions D-01..D-12 (MANDATORY before executing)
2. `.planning/phases/03-oracle-agent/03-RESEARCH.md` — Pitfalls 1-7, validation architecture, Open Questions (RESOLVED)
3. `.planning/phases/03-oracle-agent/03-VALIDATION.md` — Nyquist test map; Wave 0 gaps must be created first
4. `.planning/phases/03-oracle-agent/03-00-PLAN.md` through `03-04-PLAN.md` — execution plans (read Wave 0 first)
5. `src/oracle_protocol.py` — OracleProtocol (LOCKED), OracleReply (extensible), MockOracle
6. `src/conversation_loop.py` — where Phase 3 integration points live
7. `src/llm_provider.py` — existing LLM abstraction; OracleAgent accepts client from make_llm_client()

## Infrastructure State
- Python 3.14 (primary), Python 3.13 also present
- umap-learn 0.5.12 installed; hdbscan NOT installed (Python 3.14 incompatibility, non-blocking)
- sentence_transformers NOT installed (13 pre-existing test failures in phase2, not Phase 3 scope)
- LLM API keys: ANTHROPIC_API_KEY + OPENAI_API_KEY in .env
- Tests baseline: `pytest tests/phase2/ -m "not llm" -q` → 83 passed, 1 skipped
- Phase 3 tests: none yet (Wave 0 creates them)
- Flask + SocketIO web UI: `python web/app.py` (from project root)

<context>
Planning is done cleanly. The session started with CONTEXT.md already written from a prior discussion session, so this session was research → patterns → plan → verify → revision → verify-passed. The plans have concrete implementation details and all 12 CONTEXT.md decisions are addressed. The key non-obvious engineering constraint is Pitfall 5 (events.jsonl sidecar) — this is already baked into all relevant plans. Ready for a fresh execution session.
</context>

<next_action>
Run /gsd-execute-phase 3 to execute all 5 Phase 3 plans. Start fresh context (/clear first).
Wave 0 (03-00) creates the test scaffold — must complete before Wave 1 starts.
</next_action>
