---
context: phase
phase: 03-oracle-agent
task: null
total_tasks: null
status: in_progress
last_updated: 2026-05-11T19:00:00Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints identified. No failures encountered this session._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Changing OracleProtocol signature | OracleProtocol.reply(state, message) -> OracleReply is LOCKED from Phase 2. Any change breaks the loop and all tests. | advisory | Do not add parameters to reply(). Extend OracleReply fields instead (it is NOT frozen). |
| Mutating ClusteringState schema | ClusteringState is frozen (Phase 2 D-13). Do NOT add drift_events or any new field to it. | advisory | Drift lives in AuditLog only. OracleReply carries contradiction flags. |

<current_state>
Phase 3 discussion complete — 03-CONTEXT.md written and committed (74ee4fc).
No plans created yet. No code written yet.

All 12 implementation decisions captured (D-01 through D-12).
Ready to plan.
</current_state>

<completed_work>

**This session:**
- Discussed all 4 gray areas for Phase 3 (preference spec, noise mechanics, cognitive load, drift detection)
- Created `.planning/phases/03-oracle-agent/03-CONTEXT.md` — 12 decisions locked
- Created `.planning/phases/03-oracle-agent/03-DISCUSSION-LOG.md` — audit trail
- Committed: docs(03): capture phase context (74ee4fc)
</completed_work>

<remaining_work>

- **Plan Phase 3** — `/gsd-plan-phase 3`
- **Execute Phase 3 plans** — `/gsd-execute-phase 3`
- Requirements to implement: ORC-01, ORC-02, ORC-03, ORC-04, FB-04
</remaining_work>

<decisions_made>

All decisions in `03-CONTEXT.md`. Key ones for a fresh agent:

- `OracleSpec(preferred_k, semantic_axes, persona_description)` — injected at `OracleAgent.__init__`
- `NoiseParams(consistency_rate, drift_probability, sycophancy_resistance)` — prompt-injected natural-language rules; fixed per run; logged as `oracle_init` JSONL event
- `f_cognitive_load(state, message) → float` — pure function in `src/cognitive_load.py`; called by loop BEFORE `oracle.reply()`; formula = weighted avg of cluster count + items shown + message length; `COG_LOAD_THRESHOLD = 0.7` → "focus on one thing only"
- Drift detection: structural FeedbackDelta comparison (rolling window, zero LLM cost); logged to AuditLog as `drift_event`; surfaced via `OracleReply.contradiction_detected: bool` + `contradicted_turn: int | None`
- FB-04: InstructionalFeedback already parsed; inject via existing `global_instructions` accumulator into oracle system prompt — no new ConstraintStore
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)
1. `.planning/phases/03-oracle-agent/03-CONTEXT.md` — ALL decisions D-01..D-12 (MANDATORY before planning)
2. `.planning/ROADMAP.md` §Phase 3 — success criteria and ORC-02 logging requirement for Phase 6
3. `src/oracle_protocol.py` — OracleProtocol, OracleReply, MockOracle (Phase 3 replaces MockOracle; extends OracleReply)
4. `src/conversation_loop.py` — where f_cognitive_load call and drift logging must be wired in
5. `src/feedback_parser.py` — InstructionalFeedback already parsed here (FB-04 uses it)
6. `src/feedback.py` — FeedbackDelta types used in structural drift comparison (D-09)
7. `src/serialization.py` — append_to_audit_log() used for oracle_init and drift_event records

## Infrastructure State
- Tests: `pytest tests/phase2/ -m "not llm" --tb=no -q` — 83 passed, 1 skipped (hdbscan), baseline for Phase 3
- `ANTHROPIC_API_KEY` + `OPENAI_API_KEY` required (both in `.env`)
- No Phase 3 tests exist yet — planner creates test scaffold as first plan

<context>
Discussion just completed. 03-CONTEXT.md is the single source of truth for all Phase 3 decisions. The planner needs to read it before creating any plans.

Key non-obvious decision: f_cognitive_load is a standalone pure function called by the LOOP, not by the oracle. This is consistent with the f_* decomposition pattern from Phase 2. The oracle receives the score as a parameter injected into its system prompt — it does not compute its own load.

Key non-obvious decision: drift is detected AFTER parse_feedback runs (structural comparison of the new deltas against a rolling window of prior deltas). The oracle generates its reply first, THEN the loop detects whether it contradicts prior feedback structurally.
</context>

<next_action>
Run /gsd-plan-phase 3 to create Phase 3 plans (OracleAgent implementation).
</next_action>
