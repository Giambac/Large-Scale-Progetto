---
context: phase
phase: 02-clustering-agent-core
task: null
total_tasks: null
status: in_progress
last_updated: 2026-05-08T11:42:31Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints. The Trio requirements are new scope; there are no prior failures to avoid._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| context-bound emit() in background thread | `from flask_socketio import emit` raises RuntimeError outside request context; always use `socketio.emit()` instance method | advisory | WR-03 lock added; grep for forbidden import in acceptance criteria |
| source==target no-op move | `_apply_move_item` — fixed with early return on `source_cluster_id == target_cluster_id` | advisory | CR-01 fix already applied |
| self-merge duplicates item IDs | `_apply_merge(a, a)` doubled every item ID — fixed with assert | advisory | CR-02 fix already applied |

<current_state>
Phase 2 v1 requirements are COMPLETE as of 2026-05-07 (10/11 truths verified, 1 deferred to Phase 4).
Three Trio-tier requirements have been added to Phase 2 and have NOT been discussed, planned, or executed:
- BACK-V2-01: k-means backend alongside HDBSCAN (LLM-first optional if time allows)
- VIZ-V2-01: UMAP/t-SNE 2D projection in web UI with color-coded cluster membership
- UI-V2-01: persistent sessions — state saved to disk and resumable across server restarts

.planning/ROADMAP.md was replaced with the Trio roadmap this session (uncommitted).
</current_state>

<completed_work>

**Phase 2 v1 (all committed, bb943c8):**
- 02-01: test scaffold
- 02-02: feedback dataclasses + parser
- 02-03: HierarchyStore, f_uncertainty, MockOracle, RandomStrategy
- 02-04: f_output, f_next_best_step, f_next_state, conversation_loop
- 02-05: Flask + SocketIO debug web UI
- Code review (4 critical + 4 warning findings) + all fixes applied
- Verification: 10/11 truths verified, 1 deferred to Phase 4
- Test suite: 65/65 non-LLM tests passing

**This session (uncommitted):**
- Replaced .planning/ROADMAP.md with Trio roadmap
- Phase 2 v1 reqs marked ✅; Trio reqs added as pending
</completed_work>

<remaining_work>

- Discuss Phase 2 Trio requirements (BACK-V2-01, VIZ-V2-01, UI-V2-01)
- Plan Phase 2 Trio requirements
- Execute Phase 2 Trio requirements
</remaining_work>

<decisions_made>

- **Trio roadmap replaces old ROADMAP**: Phases 3-6 now include research-question context and human study requirements (N≥10, within-subject in Phase 6).
- **Phase 2 Trio reqs need a fresh discuss→plan→execute cycle**: They were explicitly deferred to v2 during original Phase 2 planning (02-CONTEXT.md D-15). Now in scope.
- **soft_probs as dict-of-dicts in SocketIO**: `{str(item_id): {str(cluster_id): prob}}` — positional lists break after split/merge.
- **_apply_move_item early return on source==target**: graceful, not crash.
- **Contradiction count / convergence signal deferred to Phase 4**: JUDG-02 computes these.
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)
1. `.planning/ROADMAP.md` — updated Trio roadmap; Phase 2 Trio reqs defined here
2. `.planning/phases/02-clustering-agent-core/02-CONTEXT.md` — all Phase 2 decisions (D-01..D-15); critical for understanding what was deferred and why
3. `.planning/phases/02-clustering-agent-core/02-VERIFICATION.md` — what Phase 2 v1 delivered
4. `src/clustering.py` — existing HDBSCAN implementation (BACK-V2-01 adds k-means here)
5. `src/state.py` — ClusteringState shape (UI-V2-01 needs serialization to disk)
6. `web/app.py`, `web/templates/index.html`, `web/static/main.js` — existing web UI (VIZ-V2-01 and UI-V2-01 extend this)

## Infrastructure State
- Flask + SocketIO server: `python web/app.py`
- Tests: `pytest tests/phase2/ -m "not llm" -q` — 65 passed
- `ANTHROPIC_API_KEY` required for feedback_parser + web UI background task

<context>
The Trio requirements are independent enough to plan as separate waves:
- BACK-V2-01: add KMeansBackend alongside HDBSCANBackend behind a common interface in src/clustering.py
- VIZ-V2-01: UMAP/t-SNE dimensionality reduction → new src/projection.py + web UI cluster cards get colored scatter plot
- UI-V2-01: serialize ClusteringState + AuditLog to disk each turn; restore on /resume endpoint in web/app.py

"LLM-first optional if time allows" note on BACK-V2-01 suggests k-means backend is priority; LLM-based clustering is stretch.
</context>

<next_action>
Run /gsd-discuss-phase scoped to Phase 2 Trio requirements: BACK-V2-01, VIZ-V2-01, UI-V2-01.
</next_action>
