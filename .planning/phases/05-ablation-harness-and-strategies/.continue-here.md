---
context: phase
phase: 05-ablation-harness-and-strategies
task: 0
total_tasks: 0
status: complete
last_updated: 2026-05-17T00:00:00Z
---

# Phase 5 Complete — Transition to Phase 6

_No blocking constraints._

<current_state>
Phase 5 (Ablation Harness and Strategies) is **fully complete**:
- All 5 plans executed (05-01 through 05-05)
- 68/68 tests green
- UAT 8/8 passed
- STATE.md updated to "phase-complete"

Ready to plan Phase 6: Generalization and Human Validation.
</current_state>

<completed_work>

- **05-01** — `oracle_type` DB column (docs/MODEL.md → DDL → Pydantic models + round-trip test) — `33ef904`
- **05-02** — `_format_message` enrichment with `id_to_text` (W-02 fix: empty payload ≠ deviation) — `42fb2a5`
- **05-03** — `UncertaintyDrivenStrategy` + `BoundaryDrivenStrategy` appended to `src/strategy.py` — `e363411`
- **05-04** — N×M×K harness (`src/harness.py`, `experiments/configs/harness.yaml`, `examples/run_harness.py`) — `d1e38e1`
- **05-05** — Bootstrap CI (`src/analysis.py`, `examples/compute_ci.py`, `notebooks/analysis.ipynb`) — `5ec12b5`
- **UAT** — 8/8 passed, 1 gap documented (stale DB) — `a6cc84b`
</completed_work>

<remaining_work>

- Plan Phase 6: `/gsd-plan-phase 6`
- Run the actual harness with `ANTHROPIC_API_KEY` set to produce ablation results (deferred — requires API budget decision)
</remaining_work>

<decisions_made>

- **B-02 fix**: production harness uses real `OracleAgent` + `anthropic.Anthropic` client. MockOracle only under `HARNESS_DRY_RUN=1` (1-turn convergence = no statistical power).
- **W-01 fix**: `run_baseline` invoked once per `(persona, seed)` pair — deduplicated across all 3 strategies.
- **W-04 fix**: `EmbeddingStore.compute_and_save` called once in orchestrator thread; shared read-only across all worker threads.
- **D-11**: no `ALTER TABLE` migration for `oracle_type` — fresh `experiments.db` required for Phase 5.
- **W-02 fix**: empty payload in `_format_message` is the legitimate Phase 2/RandomStrategy contract — `deviation()` NOT fired for empty payloads; only for unknown `cluster_id` or missing `item_id`.
</decisions_made>

<blockers>
None.

Two non-blocking human prerequisites before running the harness:
1. `ANTHROPIC_API_KEY` env var must be set (real oracle path).
2. Delete stale `experiments.db` at repo root if it was created before Phase 5 (lacks `oracle_type` column). Confirmed during UAT.
</blockers>

## Required Reading (for Phase 6 planning)
1. `.planning/ROADMAP.md` §Phase 6 — goal, success criteria, requirements (ALAB-03, human study N≥10)
2. `.planning/PROJECT.md` — human validation study protocol constraints
3. `.planning/phases/05-ablation-harness-and-strategies/05-CONTEXT.md` — D-12 (oracle_type filter), D-14 (comparison table) — analysis layer Phase 6 reuses
4. `CLAUDE.md` — project guardrails (fail-loudly, src/db/ SQL isolation, WAL mode)

## Critical Anti-Patterns (do NOT repeat these)
- **ANTI-PATTERN: Run harness on machine with stale experiments.db.** `CREATE TABLE IF NOT EXISTS` silently skips the new DDL — `oracle_type` column missing, runtime crash. → Always delete `experiments.db` before first harness run on any machine that ran Phase 4 code.
- **ANTI-PATTERN: Stub harness with MockOracle for real ablation results.** MockOracle returns `satisfied=True` in 1 turn — zero variance between strategies, bootstrap CI collapses to a point. → Real runs require `ANTHROPIC_API_KEY`; `HARNESS_DRY_RUN=1` is CI-only.

## Infrastructure State
- **git**: Working tree clean (except untracked `check_db.py` and `report_corrections.md` — not part of any plan). Branch `main` at `a6cc84b`, 1 commit ahead of `origin/main`.
- **experiments.db**: Deleted during UAT (stale pre-Phase-5 schema). Will be auto-created fresh on next `connect()` call.
- **ANTHROPIC_API_KEY**: Not required for Phase 6 planning; required for running the harness.
- **68 tests**: All green (`python -m pytest tests/phase5/ tests/test_db.py`).

<context>
This session executed all 5 Phase 5 plans in 4 waves (Wave 1 parallel: 05-01 + 05-02; Wave 2: 05-03; Wave 3: 05-04; Wave 4: 05-05), then ran UAT. The plan-checker's 3 round-1 fixes (B-02, W-01, W-04) were all implemented correctly. The only issue surfaced during UAT was the stale `experiments.db` — documented and resolved.

Phase 6 is the final phase: mapping oracle preferences into an evaluable function + comparing LLM vs human oracle. The `oracle_type` column (Plan 05-01) and `--oracle-type` CLI flag (Plan 05-05) were designed specifically for this.
</context>

<next_action>
Start with: `/gsd-plan-phase 6` to plan Phase 6 (Generalization and Human Validation).
</next_action>
